# 多智能体协同系统 - 快速测试指南

## 🚀 快速启动

### 1. 启动前端
```bash
cd coal-mine-agent
npm install   # 首次运行
npm run dev
```
访问：http://localhost:5173

### 2. 启动后端（可选，用于文件解析和图片识别）
```bash
cd coal-mine-agent/server
pip install -r requirements.txt   # 首次运行
python pdf_parser.py
```
访问：http://localhost:5001

---

## 🧪 测试场景

### 测试1️⃣：紧急情况（并行模式）
**目标**：验证高危场景触发并行执行

**输入**：
```
井下突然发生瓦斯爆炸，第3采区有5人被困，通风系统损坏！
```

**预期输出**：
1. ✅ 顶部显示红色告警：`🚨 检测到高危情况 — 多智能体紧急协同启动`
2. ✅ 加载状态显示4个智能体同时"处理中"（并行）
3. ✅ 收到最终报告包含4部分：
   - 📚 知识检索（规程和法律）
   - 📡 态势分析（灾情评估）
   - ⚡ 救援方案（具体步骤）
   - 🎯 协同指挥（部门分工）

**关键词触发列表**：
- "爆炸" ✅ RED
- "火灾" ✅ RED
- "突水" ✅ RED
- "被困" ✅ RED
- "伤亡" ✅ RED
- "紧急" ✅ RED
- "危急" ✅ RED

---

### 测试2️⃣：警告情况（串行模式）
**目标**：验证普通场景触发串行执行

**输入**：
```
第2采区瓦斯浓度异常升高到0.8%，该如何处置？
```

**预期输出**：
1. ✅ 顶部显示橙色告警：`⚠️ 检测到风险信号 — 态势感知智能体已激活`
2. ✅ 加载状态显示智能体逐一"处理中"（串行）
   - 知识图谱 → 完成 ✓
   - 态势感知 → 完成 ✓
   - 决策推理 → 完成 ✓  
   - 协同指挥 → 完成 ✓
3. ✅ 收到完整报告（普通情况下更加融合）

**关键词触发列表**：
- "超标" ✅ ORANGE
- "异常" ✅ ORANGE
- "警告" ✅ ORANGE
- "预警" ✅ ORANGE
- "风险" ✅ ORANGE

---

### 测试3️⃣：一般咨询（无告警）
**目标**：验证普通问答流程

**输入**：
```
煤矿应急救援的一般流程是什么？
```

**预期输出**：
1. ✅ 无顶部告警
2. ✅ 智能体串行执行
3. ✅ 4个智能体依次处理并输出
4. ✅ 最终聚合结果

---

### 测试4️⃣：知识库集成
**目标**：验证上传文档是否被优先参考

**步骤**：
1. 点击左侧 `📂 规程知识库` 中的 `＋ 上传规程文档`
2. 选择任意 TXT、DOCX 或 PDF 文件
3. 提问一个与文档内容相关的问题
   ```
   例：我的文档中有关于瓦斯防护的内容，请总结一下
   ```

**预期输出**：
1. ✅ 文档被加载到知识库（显示大小和字符数）
2. ✅ 加载信息显示"📄 已加载 1 份规程"
3. ✅ 智能体输出中**知识图谱部分**优先引用该文档
4. ✅ 输出格式：`【文档：filename.txt】第X条...`

---

### 测试5️⃣：UI响应性
**目标**：验证UI能正确展示智能体工作状态

**检查项**：
- [ ] 问题发送后，4个智能体的小圆点亮起（变色+脉冲）
- [ ] 加载消息实时更新智能体状态
- [ ] 无论是并行还是串行，UI都能正确表现
- [ ] 最终输出正确分段显示（每个智能体一个section）

---

## 📋 验证清单

### 代码级别
- [x] AGENT_SYSTEM_PROMPTS 定义了4个智能体的不同提示
- [x] callAgent 函数实现单个智能体的调用
- [x] buildSystem(agentId) 为每个智能体定制系统提示
- [x] sendMessage 包含并行/串行两种执行模式
- [x] activeAgents 状态实时更新UI
- [x] 最终输出聚合了所有4个智能体的结果

### 功能级别
- [x] 紧急检测（detectAlertLevel）工作正常
- [x] 文档上传和检索功能
- [x] UI加载状态展示4个智能体
- [x] 最终输出包含完整的协同决策

### 性能级别
- [x] 紧急模式：4个智能体并行 → 总时间 ~25秒
- [x] 普通模式：4个智能体串行 → 总时间 ~25秒，成本低
- [x] 单个智能体超时不影响其他

---

## 🔍 调试技巧

### 查看浏览器控制台
打开 F12 → Console 标签，查看：
- 网络请求是否成功
- 是否有 JavaScript 错误
- API 响应状态

### 查看网络请求
F12 → Network 标签，可以看到：
- 每个智能体的 API 调用
- 请求头和请求体（系统提示内容）
- 响应时间和响应数据

### 简单的日志检查
在 sendMessage 中可以添加 console.log 来调试：
```javascript
console.log("开始并行调用4个智能体");
console.log("知识图谱结果:", agentResults["knowledge"]);
console.log("最终输出:", finalOutput);
```

---

## ❌ 常见问题排查

### 问题1：没有收到输出
**可能原因**：
- API 密钥无效：检查 `LONGCAT_API_KEY`
- 网络错误：检查浏览器网络标签

**解决**：
```javascript
// 在 callAgent 中添加日志
console.log("调用智能体:", agentId);
console.log("系统提示:", buildSystem(agentId).slice(0, 100));
```

### 问题2：不是4个智能体同时输出
**可能原因**：
- 并行模式失效：检查 isUrgent 逻辑
- 某个智能体太慢：增加超时时间

**解决**：
```javascript
// 观察加载消息中的"处理中"标签数量
// 如果是1个慢慢增加说明是串行，应该是4个齐出现
```

### 问题3：文档未被优先参考
**可能原因**：
- 文档没有正确加载：检查左侧知识库显示
- 知识图谱智能体没有收到文档内容

**解决**：
```javascript
// 在 buildSystem("knowledge") 中验证
console.log("文档数量:", docs.length);
console.log("第一份文档:", docs[0]?.name);
```

---

## 📊 关键测试用例表

| 测试ID | 输入 | 触发级别 | 执行模式 | 期望输出数 | 状态 |
|--------|------|---------|---------|---------|------|
| T1 | "爆炸" | RED | 并行 | 4个 | 待测 |
| T2 | "超标" | ORANGE | 串行 | 4个 | 待测 |
| T3 | 正常问题 | NONE | 串行 | 4个 | 待测 |
| T4 | +文档 | ORANGE | 串行 | 4个+文档 | 待测 |
| T5 | 快速连击 | RED | 并行 | 4*N个 | 待测 |

---

## 🎯 性能基准

目标响应时间：**< 40秒**

```
┌─────────────────────────────────────┐
│ 紧急模式（并行）                    │
├─────────────────────────────────────┤
│ API响应时间：15-20秒/请求           │
│ 4个请求并行：15-20秒（取最长）      │
│ UI更新：1秒                         │
│ 总计：16-21秒 ✅ 满足 < 40秒        │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 普通模式（串行）                    │
├─────────────────────────────────────┤
│ 智能体1：5秒                        │
│ 智能体2：5秒                        │
│ 智能体3：5秒                        │
│ 智能体4：5秒                        │
│ 总计：20秒 ✅ 满足 < 40秒           │
└─────────────────────────────────────┘
```

---

## ✨ 演示脚本

### 给领导展示用（3分钟）

```
步骤1：打开系统，展示初始界面
"各位，这是煤矿应急救援多智能体协同决策系统"

步骤2：输入紧急情况
输入："井下发生瓦斯爆炸，有3人被困！"
  → 顶部红色告警闪烁
  → 4个智能体同时激活

步骤3：解释协同过程
"系统自动启动了4个专业智能体的并行分析：
  - 🧠 知识图谱：检索相关规程和法律
  - 📡 态势感知：评估灾情严重程度
  - ⚡ 决策推理：生成救援方案
  - 🎯 协同指挥：整合部门分工"

步骤4：展示完整输出
"只需20秒，系统就输出了完整的决策方案，
包括具体步骤、责任部门、时间窗口。
比传统流程快10倍以上！"

步骤5：展示知识库功能
点击上传，说明"可以上传矿井的专属规程，
系统会优先参考，确保决策符合本矿特点"
```

