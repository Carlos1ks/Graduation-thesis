# 煤矿应急救援多智能体协同系统

## 🏗️ 系统架构

### 核心设计
本系统采用**4智能体协同**架构，每个智能体负责不同的决策环节，通过串行或并行方式协同工作。

```
用户输入 → 紧急检测 → 智能体协同 → 输出聚合 → 用户展示
              ↓
           [RED]紧急 → 并行执行(快速响应)
           [ORANGE]警告 → 串行执行(成本优化)
```

---

## 👥 四大智能体

### 1️⃣ 知识图谱智能体 (🧠 Knowledge)
**职责**：检索和提取专业知识  
**输入**：用户问题 + 上传的规程文档  
**输出**：相关条款、规范、案例

**工作流程**：
```
问题 → 文档检索 → 知识抽取 → 来源标注 → 返回结构化知识
```

**知识层级**：
- 法律法规层（应急管理部规章）
- 行业规程层（煤矿安全规程）
- 具体操作层（具体应急步骤）

---

### 2️⃣ 态势感知智能体 (📡 Perception)
**职责**：全面分析灾害形势  
**输入**：用户描述的灾害场景 + 知识库  
**输出**：态势分析报告

**分析维度**（必须覆盖）：
```
灾害类型识别
    ↓
危害程度评估 (红/橙/黄/绿)
    ↓
环境因素分析 (井深、通风、地质、逃生通道)
    ↓
资源评估 (人员、设备、物资)
    ↓
时间压力判断 (分钟级/小时级/日级)
```

**输出示例**：
```
📡 态势分析：
- 灾害分类：瓦斯爆炸 | 危害等级：红
- 影响范围：井下第3采区 | 指挥级别：特别重大
- 核心制约：
  1. 人员被困，通风系统受损
  2. 瓦斯浓度持续升高，扩散风险大
  3. 救援队到达需15分钟
```

---

### 3️⃣ 决策推理智能体 (⚡ Decision)
**职责**：生成可执行的救援方案  
**输入**：知识检索结果 + 态势分析  
**输出**：完整的救援决策

**方案设计原则**：
```
优先级：立即救人 > 防止扩散 > 善后处理

每步明确：
  - 具体行动  
  - 责任部门
  - 时间窗口
  - 人员配置
  - 所需装备
```

**输出示例**：
```
⚡ 救援方案决策：

【主方案】
- 第1步（当下，0-5分钟）：启动应急指挥 | 责任：矿长办公室 | 通知消防部门
- 第2步（5-10分钟）：调整通风 | 责任：生产科 | 检查空气质量
- 第3步（10-20分钟）：组织救援队进入 | 责任：救援队长 | 配备全面防护

【备选方案】
- 如果通风系统无法修复，则采用被动充气方案

【风险提示】
- 瓦斯再爆风险（全程监测）
- 人员缺氧（必须携带氧气瓶）
```

---

### 4️⃣ 协同指挥智能体 (🎯 Coordination)
**职责**：整合所有智能体输出，生成完整协同方案  
**输入**：前3个智能体的所有输出  
**输出**：指挥官可直接用的协同方案

**协同内容**（必须包含）：
```
1. 指挥链条（矿长 → 副总 → 各科室）
2. 部门分工表（救援队、调度、医疗、安全各自做什么）
3. 5分钟关键指标（需要不断汇报什么）
4. 资源调配优先级（谁先出动，物资怎么分配）
5. 升级/降级条件（什么情况下提升指挥等级）
```

**输出示例**：
```
🎯 协同指挥方案（5分钟内启动）：
│
├─ 现场指挥官：安全科科长 → 目标：30min内救出全部被困人员
├─ 汇报链条：现场 → 调度室 → 矿长 → 应急办
│
├─ 分工执行表：
│   ├─ 救援队（组长：王队长）：
│   │   ├─ 第1步：集合装备检查（0-2min）
│   │   ├─ 第2步：进入井下（2-5min）
│   │   └─ 第3步：搜救与撤离（5-20min）
│   │
│   ├─ 调度室（组长：张调度）：
│   │   ├─ 通风调整（即刻）
│   │   ├─ 人员清点（2min内）
│   │   └─ 情报汇总（全程）
│   │
│   ├─ 医疗组（组长：李医生）：
│   │   ├─ 待机位置：井口西侧100m
│   │   ├─ 分类标准：分红/黄/绿三级
│   │   └─ 转运路线：直达县医院
│   │
│   └─ 安全科（组长：刘科长）：
│       ├─ 全面停工（即刻）
│       ├─ 风险评估（5min内）
│       └─ 撤离范围：全矿井
│
├─ 5分钟关键汇报指标：
│   - 被困人数与位置
│   - 通风系统状态
│   - 瓦斯浓度
│   - 救援队进展
│   - 伤员情况
│
└─ 升级条件：
    - 如果救援队遇阻 → 升级为省级指挥
    - 如果人数增加 → 升级资源等级
```

---

## 🔄 协同流程（两种模式）

### 模式1️⃣：紧急模式（RED级别）
**触发条件**：检测到"爆炸、火灾、突水、被困、伤亡"等关键词

```
用户输入 
    ↓
[并行执行]
├─ 智能体1(知识) → 1000字知识检索
├─ 智能体2(态势) → 1000字态势分析  
├─ 智能体3(决策) → 1000字救援方案 (并行)
└─ 智能体4(指挥) → 1000字协同方案 (并行)
    ↓ [Promise.all - 全部完成]
最终聚合报告
    ↓
用户收到完整方案（总耗时：20-30秒）
```

**优势**：并行执行，快速响应紧急情况

---

### 模式2️⃣：普通模式（ORANGE/YELLOW级别）
**触发条件**：检测到"超标、异常、警告、预警、风险"等关键词或无关键词

```
用户输入
    ↓
[串行执行]
知识图谱(5秒) → 知识输出 ✓
    ↓ [历史消息+知识]
态势感知(5秒) → 态势输出 ✓
    ↓ [历史消息+知识+态势]
决策推理(5秒) → 方案输出 ✓
    ↓ [历史消息+知识+态势+决策]
协同指挥(5秒) → 指挥方案 ✓
    ↓
用户收到完整方案（总耗时：20-30秒，成本低）
```

**优势**：
- 成本低（4次API调用）
- 后续智能体可以参考前序智能体的输出
- 产生更加一致和融合的建议

---

## 📊 数据流

```
消息历史记录 (role, content)
        ↓
    [清理标记]
        ↓
┌─────────────────────────────────────┐
│  为每个智能体构建系统提示            │
│  + 用户问题                         │
│  + 历史上下文                       │
│  + 相关规程文档                     │
└─────────────────────────────────────┘
        ↓
    [并行/串行调用]
        ↓
   [收集结果]
        ↓
┌─────────────────────────────────────┐
│  格式化聚合输出                      │
│  1. 知识图谱输出                    │
│  2. 态势感知输出                    │
│  3. 决策推理输出                    │
│  4. 协同指挥输出                    │
│  5. 协同耗时/执行模式               │
└─────────────────────────────────────┘
        ↓
    [发送给用户]
```

---

## 🎯 关键代码逻辑

### 1. 智能体配置
```javascript
const AGENT_SYSTEM_PROMPTS = {
  knowledge: { name: "知识图谱智能体", prompt: (docs) => {...} },
  perception: { name: "态势感知智能体", prompt: () => {...} },
  decision: { name: "决策推理智能体", prompt: () => {...} },
  coordination: { name: "协同指挥智能体", prompt: () => {...} }
}
```

### 2. 单次智能体调用
```javascript
const callAgent = async (agentId, userText, history) => {
  // 为特定智能体构建系统提示
  const systemPrompt = buildSystem(agentId);
  
  // 发起API请求
  const res = await fetch(LONGCAT_API_URL, {
    system: systemPrompt,
    messages: [...history, { role: "user", content: userText }]
  });
  
  // 返回智能体的输出
  return textBlock.text;
}
```

### 3. 协同调度逻辑
```javascript
// 并行模式（紧急）
if (isUrgent) {
  const promises = agentOrder.map(agentId =>
    callAgent(agentId, userText, history)
  );
  const results = await Promise.all(promises);  // 全部并行执行
}

// 串行模式（普通）
else {
  for (const agentId of agentOrder) {
    agentResults[agentId] = await callAgent(agentId, userText, history);
    // 后续智能体可以看到前序输出
  }
}
```

### 4. 输出聚合
```javascript
const finalOutput = `🤖 多智能体协同决策结果

${agentResults["knowledge"] ? `## 📚 知识...` : ""}
${agentResults["perception"] ? `## 📡 态势...` : ""}
${agentResults["decision"] ? `## ⚡ 决策...` : ""}
${agentResults["coordination"] ? `## 🎯 指挥...` : ""}

---
⏱️ 协同耗时：${isUrgent ? '并行' : '串行'}执行`;
```

---

## 💡 使用场景

### 场景1：瓦斯爆炸事故（高危）
```
用户：井下瓦斯爆炸，有2人被困！
  ↓
[检测到"爆炸、被困" → RED级别]
  ↓
[并行执行4个智能体（15秒）]
  ↓
系统输出：
  1. 知识：国家矿山应急标准、爆炸处理规程
  2. 态势：地点、气体浓度、影响范围、时间压力
  3. 决策：具体救援步骤、人员需求、装备清单
  4. 指挥：指挥链条、部门分工、5分钟指标
```

### 场景2：瓦斯浓度异常（普通）
```
用户：第2采区瓦斯浓度升至0.8%，怎么处置？
  ↓
[检测到"超标、异常" → ORANGE级别]
  ↓
[串行执行4个智能体（25秒，成本低）]
  ↓
系统输出：
  1. 知识：正常瓦斯浓度标准、通风规范
  2. 态势：影响范围小、风险级别、扩散可能
  3. 决策：短期应对（加强通风）、长期方案
  4. 指挥：一般应急程序、通风调整步骤
```

---

## 📈 性能指标

| 指标 | 紧急模式 | 普通模式 |
|------|---------|---------|
| 执行方式 | 并行 | 串行 |
| 响应时间 | 20-30秒 | 20-30秒 |
| API调用数 | 4次/并行 | 4次/串行 |
| 成本 | 高 | 低 |
| 一致性 | 独立分析 | 逐步融合 |
| 适用场景 | 紧急救援 | 日常咨询 |

---

## 🔧 扩展性

### 如何添加第5个智能体？
```javascript
// 1. 在AGENT_SYSTEM_PROMPTS中添加
collaboration: {
  name: "协作评估智能体",
  prompt: () => `...你的系统提示...`
}

// 2. 在agentOrder中添加
const agentOrder = ["knowledge", "perception", "decision", "coordination", "collaboration"];

// 3. 在聚合输出中添加
const finalOutput = `...
${agentResults["collaboration"] ? `## 🤝 协作评估输出...` : ""}
...`;
```

### 如何自定义优先级？
```javascript
// 根据不同灾害类型选择不同的智能体顺序
const agentOrder = isWaterDisaster 
  ? ["perception", "knowledge", "decision", "coordination"]  // 先感知
  : ["knowledge", "perception", "decision", "coordination"]; // 先检索
```

---

## ✅ 测试清单

- [ ] 紧急消息（含"爆炸、火灾"等）触发RED级别并行执行
- [ ] 警告消息触发ORANGE级别串行执行  
- [ ] 每个智能体能收到前序智能体的输出
- [ ] 最终聚合输出包含4个智能体的结果
- [ ] UI正确展示正在工作的智能体
- [ ] 上传文档后知识图谱优先检索
- [ ] 超时处理：单个智能体超时不影响其他

---

## 🚀 下一步改进

1. **智能体学习**：记录每个决策的实际救援效果，反馈给智能体优化提示
2. **场景库**：为不同灾害类型预配置最优的智能体执行顺序
3. **实时监控集成**：接入矿井实时传感器数据（瓦斯浓度、温度等）
4. **多模态输入**：支持语音、图片、视频上传，增强态势感知
5. **AI评估**：第5个智能体评估前4个输出的一致性和可行性

